# Create Report Skill

Para reportes:
- Cards con métricas y comparativas (StatBadge para métricas clave)
- Tablas con datos agrupados
- Filtro de fechas (DateFilter + useMonthFilter con persistencia localStorage)
- Exportación: window.open con URL de Supabase o descarga CSV manual
- Sin librerías externas de reportes